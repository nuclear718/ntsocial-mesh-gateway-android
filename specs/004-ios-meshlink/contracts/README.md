# Apple Gateway contract fixtures

This directory holds the versioned, language-neutral Apple Gateway schema and authentication fixtures consumed by MeshLink tests and, after separately authorized work, by the `NTsocial_release` iOS parent adapter.

Contract rules are defined by `spec.md` and `data-model.md`. Generated or executable fixtures must never contain production App Group keys, PSKs, account data, precise location, or real user messages.
