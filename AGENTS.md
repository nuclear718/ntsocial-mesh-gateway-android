# NTsocial MeshLink Android - Unified Agent & Developer Guide

<role>
You are an expert Android/KMP engineer working on NTsocial MeshLink, a GPL-3.0 fork of Meshtastic Android. Maintain architectural boundaries, preserve upstream-compatible Meshtastic radio/service/database/settings behavior, and use MAD standards with Compose Multiplatform + Navigation 3.
</role>

<context_and_memory>
- **Project Identity:** App display name is `NTsocial MeshLink`. Application ID is `com.ntsocial.meshlink`. Project-owned source packages use `com.ntsocial.meshlink.*`.
- **Current Status:** Gateway v1 is implemented: protected Provider snapshots, single-use command capabilities, explicit command/event IPC, canonical NTsocial channel provisioning, and an in-memory envelope cache are concrete code. Connected-radio queue validation and three-phone parent-App interoperability were completed on earlier debug artifacts; the current cloud-free/no-map artifact still needs its own device smoke test. RF scheduler expansion, node policy, persistent/reliable delivery, MeshCore transport, and remote RF-reception verification remain follow-up work.
- **Project Goal:** Build an open-source transport bridge between the NTsocial App and Meshtastic radios. The NTsocial App owns social UX and canonical history; this Gateway owns Meshtastic radio control, LoRa transport, cache, matching, node policy, and IPC.
- **Upstream Base:** This fork is based on `meshtastic/Meshtastic-Android`. Preserve upstream attribution and compatibility unless a scoped NTsocial change explicitly requires divergence.
- **Tech:** Kotlin 2.3+ with JDK 21, Ktor, Okio, Room KMP, DataStore, Koin 4.2+, Compose Multiplatform, Material 3 Expressive, and Navigation 3.
- **Local Build Status (2026-07-18):** After removing rendered maps and cloud diagnostics, `spotlessApply spotlessCheck detekt assembleDebug test allTests --continue --no-configuration-cache` passes (`BUILD SUCCESSFUL` in 4m41s; 1,569 actionable tasks), and `kmpSmokeCompile :app:lintFdroidDebug :app:lintGoogleDebug --continue --no-configuration-cache` passes (`BUILD SUCCESSFUL` in 1m18s; 932 actionable tasks). This verifies formatting, static checks, compilation, tests, KMP smoke compilation, lint, and both debug flavors; it does not prove Play distribution or remote RF delivery.
- **Android Packaging Status (2026-07-18):** The rendered-map module and its Google Maps, osmdroid, GeoPackage, and related native dependency path are removed. Both current arm64 debug APKs pass `zipalign -c -P 16`; the Google arm64 APK's five packaged ELF libraries all have 0x4000-aligned `PT_LOAD` segments. Both flavors also pass merged-manifest cloud-runtime guards. Repeat the audit after every native-dependency change and on the final signed delivery artifact.
- **Parent Interop Status (2026-07-17):** Debug MeshLink trusts only `com.ntsocial.android.debug` when the debuggable host and parent share a signer digest; release pairing remains package- and signer-pinned. The pre-cloud-removal MeshLink and NTsocial parent debug builds were clean-installed on three Android 16 phones. Provider status, explicit parent-to-MeshLink launch, lifecycle switching, primary screens, and parent text-entry/sync paths completed without crash, ANR, fatal log, or trust failure. The no-radio status correctly remained "preparing the NTsocial radio channel." This does not validate the current artifact or Play app-signing certificate pairing.
- **Play Build Status (2026-07-18):** `:app:bundleGoogleRelease` passes R8, Lint Vital, dependency/manifest cloud-runtime guards, and AAB packaging without Google Cloud, Maps, Firebase, Crashlytics, Datadog, or ML Kit configuration. The locally generated AAB is unsigned because no upload keystore is present, so it is a release-pipeline artifact only and is not Play-uploadable. Never present an unsigned artifact or debug signature as Play-ready.
- **Play Submission Status (2026-07-18):** The App is **not Production-submission-ready**. Known code/policy gates are: make the location FGS type depend on explicit phone-location opt-in or remove that capability; resolve Android 17/API 37 minimum-scope/location-button behavior; implement first-send terms acceptance plus in-app UGC reporting and effective block/ignore; remove or rewrite stale localized `analytics_notice` text; publish final policy URLs and update the in-app `privacy_url`; create and back up an upload key; synchronize Play app-signing certificates with both Apps' signer trust; prepare current store assets/Console declarations; complete cloud-free Internal-track device testing; and satisfy account-specific closed-testing/Production-access requirements. `docs/google-play/README.md` and `docs/google-play/06-first-play-launch-plan-zh-TW.md` are the submission source of truth.
- **Agent Memory:** Consult `.agent_memory/session_context.md` for the latest task-specific handovers and project state.
- **Skills Directory (CONSULT THESE FIRST):**
  - `.skills/project-overview/` - Codebase map, namespacing, **Bootstrap Steps**.
  - `.skills/kmp-architecture/` - Expect/actual, source-sets, conventions.
  - `.skills/compose-ui/` - Adaptive UI, **String Resources (consult `strings-index.txt` first)**.
  - `.skills/navigation-and-di/` - Navigation 3 & Koin annotations.
  - `.skills/testing-ci/` - Validation commands, **CI Architecture**.
  - `.skills/ci-cost-control/` - **CI Budgeting & Monitoring**.
  - `.skills/implement-feature/` - Feature workflow.
  - `.skills/code-review/` - **PR & Commit Hygiene**, validation checklist.
  - `.skills/new-branch/` - Branching and rebasing recipes.
  - `.skills/speckit/` - **Spec Kit SDD workflow**, slash commands, constitution, feature specs.
</context_and_memory>

<architecture_boundaries>
- **Project Namespace:** New project-owned code must use `com.ntsocial.meshlink.*`. Do not introduce new `org.meshtastic.*` or `com.geeksville.mesh` project packages.
- **Protocol Boundary:** Keep generated upstream Meshtastic protobufs under `org.meshtastic.proto`. Do not edit `core/proto` unless the user explicitly requests upstream protocol/submodule work.
- **Semantic Meshtastic Names:** Existing identifiers such as `MeshtasticNavDisplay`, `MeshtasticNavigationSuite`, `MeshtasticBleConstants`, and `MeshtasticDatabase` may remain when they describe upstream protocol, device, database, or shell semantics. Do not perform cosmetic class renames without a scoped migration plan.
- **Gateway Boundary (implemented v1):** New NTsocial integrations must use the protected Gateway IPC. `NtsocialGatewayService`/AIDL remains a deprecated compatibility adapter only; do not add features that bind it or directly bind `IMeshService`. New outbound traffic uses `PRIVATE_APP / port 256`; legacy `497` is receive-only compatibility.
- **Gateway Provider:** The authority is `${applicationId}.gateway`; v1 read-only endpoints are `/v1/status`, `/v1/envelopes`, `/v1/nodes`, and `/v1/channels`. Do not add unversioned endpoints, mutations, selection/sort semantics, or canonical NTsocial history to this Provider.
- **Gateway Commands:** A parent App first obtains a short-lived, single-use capability tied to `request_id` from the Provider, then sends an explicit `com.ntsocial.meshlink.gateway.COMMAND`. Android 8-13 relies on that consumed capability; Android 14+ also verifies the broadcast sender UID, package, and certificate. Never trust caller-supplied package names or ports, and never use implicit broadcasts.
- **Gateway Events:** `com.ntsocial.meshlink.gateway.EVENT` is explicitly package-scoped and metadata-only. It may carry event type, URI, request ID, packet ID, or rejection reason; it must never carry message bytes, destinations, precise location, PSKs, tokens, or radio configuration. A client re-queries the Provider after an event.
- **Trusted Pairing:** The in-process UID/package/certificate verifier is authoritative. A debuggable MeshLink host accepts `com.ntsocial.android.debug` only when parent and host share at least one signer digest; ordinary signature-permission grant is not sufficient by itself. Release remains pinned to the trusted release package/signer. A parent package or signer change requires a synchronized update to verifier rules, known-signer resources, package visibility, and both build configurations; never broaden this to arbitrary installed Apps or restore a machine-specific debug digest resource.
- **Outbound Transport Contract:** External commands accept only a validated complete `NM` envelope, unchanged, on the canonical default RF channel and `PRIVATE_APP / port 256`; the complete external envelope maximum is 180 bytes. `497` is never an outbound command port.
- **Cache and Acceptance Semantics:** The bridge cache is transient, in-memory, and bounded to 128 envelopes; NTsocial remains the social UX and canonical-history owner. `COMMAND_ACCEPTED` means MeshLink accepted a command into the local radio send queue, not that a remote radio received it.
- **Node Policy:** `rebroadcast_mode = ALL` is an NTsocial Node Policy applied with clear user consent and verification, not a silent forced configuration.
- **Channel Model:** NTsocial `channelId` is the logical route. Meshtastic `channelIndex` is the RF lane. Many-to-one channel binding is valid.
- **Parent Channel Opt-In:** MeshLink default-channel readiness must never auto-bind, restore, or choose a parent NTsocial logical channel. The parent owns the user's per-channel LoRa on/off decision; MeshLink only verifies that an opted-in command uses the canonical ready RF index.
- **Built-In NTsocial Channel:** NTsocial MeshLink must bundle the canonical public NTsocial Meshtastic channel and automatically ensure it is registered on every connected node after node DB readiness. Provisioning may update an existing NTsocial-named or same-PSK slot, add a secondary slot, replace the last secondary when full, and replace primary only on one-channel radios. This automatic channel registration does not require a user confirmation dialog.
- **LoRa Config Preservation:** Built-in NTsocial channel provisioning may apply the QR LoRa/RF config only when the radio is effectively unconfigured, such as missing LoRa config or `region == UNSET`; never overwrite an already configured region/frequency/preset.
- **LoRa Payload Policy:** Do not route image bytes, voice bytes, or PTT media over LoRa. LoRa roadmap payloads are text, state/control, profile/channel snapshots, roster/probe data, chunks, and receipts.
- **Open-Source Boundary:** This GPL fork must not embed closed NTsocial App business logic, private assets, secrets, or production credentials. Protocol/schema/IPC work intended for this Gateway should remain open.
- **Restricted Data Surface:** Raw envelope bytes are available only to a verified Provider client. Node/channel snapshots must not grow to expose positions, PSKs, RF configuration, notes, or raw protobufs, and none of those values may appear in events or logs.
- **Cloud Runtime Boundary:** Neither Android flavor may package Google Cloud, Maps, Google Play services, Firebase, Crashlytics, Datadog, ML Kit, Advertising ID/AdServices, Install Referrer, or equivalent telemetry/runtime configuration. The `google` flavor label identifies the Play publication path and never authorizes a Google service dependency. It still uses the non-Google Meshtastic project `ApiServiceImpl` for device/firmware information, while `fdroid` intentionally falls back to bundled JSON; do not assume the flavors are otherwise behaviorally identical. QR/barcode decoding is local ZXing; rendered maps are intentionally absent. Android platform location and Meshtastic radio/MQTT location controls remain because they are independent app capabilities, not Google cloud services.
- **Analytics Compatibility Seam:** `PlatformAnalytics` and existing `track()` call sites remain only to preserve upstream-compatible manager contracts. Every Android flavor must resolve them to `NoopPlatformAnalytics`, which never records, stores, or transmits events. Do not add a network-backed implementation or treat these call sites as evidence that telemetry still exists.
- **Play Data Disclosure Boundary:** No publisher analytics/crash backend does not mean that Play Data safety can claim "no collection." Mesh messages, names/IDs, optional phone location, and user-configured transports can leave the Android device. Shared-PSK channels, Ham mode, or unencrypted user-configured endpoints also prevent a blanket claim that every collected data type is encrypted in transit. Keep the Data safety form, privacy policy, prominent disclosures, and final network behavior aligned; use the conservative current draft until artifact-level testing proves a documented exception.
- **Location FGS Release Boundary:** The current `MeshService` adds the location foreground-service type whenever location permission exists, even when phone-location sharing is not enabled. Do not describe this as compliant or hide it with Console text. Before Production, either bind location acquisition/FGS type to explicit user opt-in with minimum necessary permission and a stop path, or remove phone-location sharing and the location FGS from the first release.
- **UGC Release Boundary:** Before Production, users must accept the applicable terms before first sending UGC, have an in-app content/user reporting path, and have an effective block/ignore path for one-to-one interaction. Public Markdown pages, GitHub issues, or a support email are backups, not substitutes. Do not publish the draft Terms/Community pages as final until these flows exist and have been tested.
- **Store Metadata Boundary:** `docs/google-play/` is authoritative for the cloud-free first release. Do not upload legacy Fastlane copy/screenshots that still show maps, analytics, or official-Meshtastic claims. Policy Markdown files are drafts until their warning blocks are removed, hosted on stable public HTTPS pages, and wired back into the final App artifact.
- **Device Validation (2026-07-10):** A normal NTsocial LoRa-enabled channel was tested on device through parent -> Provider/capability -> explicit command -> MeshLink -> connected radio firmware queue. The run used only port 256 and did not use AIDL or outbound 497. This proves local radio-queue acceptance, not remote over-air reception; the latter needs a second receiver or a return message.
- **Three-Phone No-Radio Validation (2026-07-17):** Fixed debug APKs were clean-installed on `SM-S9080`, `SM-S9280`, and `OPPO CPH2695`, all Android 16/API 36 arm64 devices. Connections, Messages, Nodes, the then-present Map screen, MeshCore, Settings, onboarding, parent Provider status, explicit App launch, repeated foreground/background switching, and relevant log windows passed. English-keyboard text entry was re-tested successfully, and parent-local/BLE message synchronization was observed across phones. Rendered maps have since been removed, so the current cloud-free artifact still needs its own device smoke test. Because no Meshtastic or MeshCore hardware node was available, no current-run radio connection, LoRa transmission, RF reception, remote receipt, or MeshCore transport claim may be inferred from this result.
</architecture_boundaries>

<ui_branding>
- **Current Skin:** Phase 1 NTsocial skinning is design-token based. Non-Dynamic themes use NTsocial indigo primary, emerald secondary, amber status emphasis, gray surfaces, and mixed monospace typography for compact metadata.
- **Theme Contracts:** Preserve `AppTheme(darkTheme, dynamicColor, content)`, `MaterialExpressiveTheme`, `MODE_DYNAMIC`, Dynamic Color behavior, `ThemePickerDialog`, `UiPrefsImpl`, and existing theme-selection flow.
- **Navigation Shell:** Keep the current `MeshtasticNavigationSuite`, `MeshtasticNavDisplay`, Navigation 3, and adaptive shell unless the user explicitly asks for a navigation redesign.
- **Branding Assets:** The established primary mark is the NTsocial butterfly. MeshLink launcher, store, splash, and in-app logo variants keep that silhouette and use Meshtastic's official green `#67EA94` on black. Do not reuse the upstream Meshtastic mountain logo as primary NTsocial branding. Known debt: `feature/widget/src/main/res/drawable/widget_app_icon.xml` is still the upstream mountain and is actively used by `LocalStatsWidget`; replace it before claiming the in-app/store asset set is fully rebranded. The unused legacy `app/src/main/res/drawable-anydpi/ic_splash.xml` is also not an approved primary asset.
- **Public Docs:** Public identity docs should be truthful about implementation status. Traditional Chinese-first wording is preferred for project-facing identity docs, with concise English summaries where useful.
</ui_branding>

<process_essentials>
- **Think First:** Read only what you need. Consult indices such as `strings-index.txt` before reading large resource files.
- **Hygiene:** Run `python3 scripts/sort-strings.py` after adding string resources to maintain organization and update the index.
- **Memory Persistence:** Update `.agent_memory/session_context.md` at the end of every session or major task.
- **Bootstrap First:** Run the mandatory bootstrap steps in `.skills/project-overview/SKILL.md` before any Gradle build.
- **Validation Environment:** Use JDK 21 and a valid `ANDROID_HOME`. For local validation, set `JAVA_TOOL_OPTIONS="-Duser.language=en -Duser.country=US"` when tests depend on English resources.
- **Gradle JVM Compatibility:** Root `gradle.properties` intentionally uses `-XX:+UseG1GC`. The currently configured Android Studio JBR 21 reports `Option -XX:+UseZGC not supported`; do not restore `UseZGC` or `ZGenerational` unless the exact build JVM has first been verified to support them.
- **16 KB Native Page Compatibility:** The former osmdroid/GeoPackage native path is removed. After changing any remaining native dependency, audit the produced arm64 APK with `zipalign -c -P 16` and verify every packaged ELF's `PT_LOAD` alignment before device or Play delivery.
- **Navigation Assembly Tests:** Tests that only verify Navigation 3 entry-provider registration should construct the `NavBackStack` and providers directly. Do not launch Robolectric Activity/Compose infrastructure for this contract; it previously caused a one-minute `UncompletedCoroutinesError` cleanup flake under the full parallel baseline despite correct navigation assembly.
- **Plan Before Execution:** Use `.agent_plans/` (git-ignored) for complex refactors.
- **Baseline Verification:** Always run `./gradlew spotlessApply spotlessCheck detekt assembleDebug test allTests` for implementation changes. KMP, flavor, navigation, dependency, or host-wiring changes also require `./gradlew kmpSmokeCompile :app:lintFdroidDebug :app:lintGoogleDebug`. Use `--no-configuration-cache` when cache or stale problems-report issues interfere.
- **Play Release Verification:** After any Android dependency, manifest, signing, store-facing resource, or release-path change, run `./gradlew :app:verifyGoogleReleaseNoCloudRuntimeDependencies :app:bundleGoogleRelease` in addition to the applicable baseline. Verify the merged-manifest guard, upload certificate, Play app-signing certificate, `bundletool validate`, 16 KB native alignment, App Bundle Explorer contents, and Play-installed artifact; a successful local bundle task alone is never enough.
</process_essentials>

<rules>
- **Token Hygiene:** Never read binary files such as PNG, MP3, APK, or large non-code resources unless essential. Use file paths to reason about assets.
- **Context Discipline:** Limit context to relevant modules. Do not vacuum the entire codebase for localized fixes.
- **No Lazy Coding:** Do not use placeholders such as `// ... existing code ...`. Provide complete, valid code.
- **No Framework Bleed:** Never import `java.*` or `android.*` in `commonMain`. Use KMP equivalents such as Okio, `Mutex`, and atomicfu.
- **CMP Over Android:** Use Compose Multiplatform constraints. Pre-format floats with `NumberFormatter.format()`. Use `MeshtasticNavDisplay` and `NavigationBackHandler`.
- **Zero Lint Tolerance:** Work is incomplete if `detekt` or `spotlessCheck` fails.
- **Verify Before Push:** Treat any push as verify-then-push. Check GitHub Actions state before pushing when GitHub context is relevant.
- **Never Touch Protos or Secrets:** `core/proto` is an upstream submodule. Secrets are git-ignored and must not be logged, committed, or exposed.
- **Privacy First:** Never log or expose PII, precise location, private messages, cryptographic keys, tokens, or pairing credentials.
- **Truthful Status:** Do not describe planned NTsocial gateway features as shipped. Mark roadmap, docs, and UI placeholders clearly when behavior is not implemented yet.
</rules>

<documentation_sync>
`AGENTS.md` is the source of truth for rules and principles. `.github/copilot-instructions.md` provides a quick-reference subset optimized for Copilot sessions, including build commands, task naming, and conventions. `CLAUDE.md` and `GEMINI.md` redirect here. When this guide changes in a way that affects day-to-day commands or naming, keep the quick-reference docs aligned.
</documentation_sync>

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan.
<!-- SPECKIT END -->
