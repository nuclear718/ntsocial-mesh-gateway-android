# Historical agent status through 2026-09-04

> Archived on 2026-09-05 from `AGENTS.md` at `99bc567d9777adb15e17133403afda630bcaaa56`.
> These quoted paragraphs preserve historical artifact/test claims, including superseded conclusions.
> They are not current operating instructions. Use [AGENTS.md](../../AGENTS.md) and the
> [current audit](../development-status-audit-2026-09-05.md) for present capability and limitations.
> A past statement that a test was not performed applies to that run only.

> - **Android Track Status:** Gateway v1 remains implemented and immutable. Additive Gateway v2 provides protected status, a complete configured primary/secondary channel catalog, bounded native broadcast-text insertion history, metadata-only wakeups, caller/source/slot/generation-bound routes, Room schema 43 identity/origin capture, and Room + WorkManager + ledger admission for both complete NTsocial overlay envelopes and broadcast native Meshtastic text on existing channels. Native text is inserted into MeshLink's normal channel history; NTsocial remains canonical-history owner for NTsocial-native encrypted channels. The first-release shared Connections UI exposes Bluetooth only; USB/TCP discovery and transport capabilities remain backend-only. The current Gateway v2/cloud-free/no-map/Bluetooth-only Debug artifact now has retained four-phone install/startup and active Room 42-to-43 migration evidence, but still needs a connected-radio Provider/caller admission run and two-radio interoperability evidence. RF scheduler expansion, node policy, MeshCore transport, and remote RF-reception verification remain follow-up work.

> - **Android Meshtastic Multi-Node Phase-1 Source Status (2026-08-24):** The `multi_nodes_` source now supports a durable catalog of at most four distinct Meshtastic BLE endpoints, serialized full-session bootstrap, address-keyed active BLE ownership, and one independent Room database, radio/config DataStore set, Koin scope, service/repository/packet graph, connection generation, and queued-message drain per secondary endpoint. The existing root graph remains the immutable legacy-primary compatibility facade for Gateway v1/v2, Android host notifications/widgets/phone-location ownership, and existing callers; secondary endpoints fail closed for Gateway access. Connections exposes the four-radio fleet, and every endpoint-aware Android top-level feature, including Messages/channel history and Settings, shows address-last-four tabs backed by endpoint-scoped ViewModels. Endpoint switching deliberately returns that feature to its root destination; independent retained child back stacks are not implemented. App-global preferences remain shared even when rendered inside each endpoint's Settings tab. MeshCore multi-node, Gateway v3 routing, a global cross-endpoint ledger/scheduler, and multi-endpoint Android host services are not part of this phase. At that snapshot, the shared contracts and address-keyed BLE registry compiled for Desktop/JVM and iOS Simulator while Windows and iOS retained single-radio UI/runtime behavior; the 2026-08-30 iOS phase below supersedes the iOS part of that limitation. Focused fleet, persistence, database pinning, BLE ownership, and Koin-scope tests pass. The JDK-21/en-US root `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile --continue` gate completed 1,685 actionable tasks (251 executed, 1,434 up-to-date); formatting, both Android Debug assemblies, tests, `allTests`, Desktop/JVM, shared KMP, and iOS Simulator compilation passed, and the command was nonzero only for the six documented pre-existing Detekt findings in unmodified BLE (3), domain precise-location sharing (1), model (1), and network (1) sources. Google and F-Droid Debug lint both pass (770 actionable tasks: 105 executed and 665 up-to-date). This is source/fake-test evidence only: no two- or four-radio Android hardware run, independent channel mutation, reconnect storm, Doze/background, RF delivery, or remote-receipt validation has been performed.

> - **Android Simultaneous Multi-Node BLE Remediation Status (2026-08-31):** The Android secondary-endpoint production graph no longer uses constructor-reference Koin registrations for constructors that require qualified `ProcessLifecycle`/`ServiceScope` values or Kotlin `Lazy<T>` dependencies. Explicit scoped factories now preserve those semantics across the complete connection, packet, router, configuration, and channel-reliability graph; secondary Gateway access is explicitly bound to the fail-closed `SecondaryGatewayRepository`, and graph wiring is marked complete only after successful resolution so a failed first attempt cannot poison retries. A Robolectric production-graph regression test resolves a real secondary `MeshConnectionManagerImpl`, asserts the fail-closed Gateway binding, and closes the endpoint resources; existing fleet tests continue to cover four independent endpoint sessions and the hard maximum of four. Focused App tests, `:app:detekt`, and Google Debug assembly pass. The JDK-21/en-US full `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile :app:lintFdroidDebug :app:lintGoogleDebug --continue` gate completed 2,018 actionable tasks (394 executed, 2 from cache, 1,622 up-to-date) in 2m31s; formatting, both Android Debug assemblies, tests/`allTests`, Desktop/JVM, shared KMP/iOS Simulator compilation, and both Android lints passed, with exit 1 only for the six recorded pre-existing Detekt findings in unmodified BLE (3), domain (1), model (1), and network (1) sources. The final 52,961,456-byte Google arm64 Debug APK has SHA-256 `19CF41C5125DDA5970229A43D35C9920459294CD7BEBEAB7387E5AF6167D019D` and was data-preserving installed on an Android 16 SM-S9280. On that phone, two bonded Meshtastic radios simultaneously completed Stage 2 and remained represented by two distinct MeshLink GATT clients; disconnecting and reconnecting only the secondary removed and recreated only its client while the primary stayed connected, and the final full-gate APK again brought both radios through Stage 2 and retained both connections with one stable App PID through a 30-second background check. This proves the reported two-radio Android failure is corrected on the available hardware and supports the one-to-four source capacity claim, but it is not three/four-radio hardware, RF-send/remote-receipt, Doze, Profile/Release-device, signed-release/store, Windows-device, or iOS-device evidence. Full report: `ANDROID_SIMULTANEOUS_MULTI_NODE_BLE_REMEDIATION_REPORT_2026-08-31.md`.

> - **Android Three-Phone Multi-Node P1 Remediation Status (2026-08-25):** The four confirmed failures from `ANDROID_MULTI_NODE_THREE_PHONE_HARDWARE_TEST_REPORT_2026-08-24.md` were independently reproduced and minimally corrected. Nested Channels/Firmware/Wi-Fi deep links now append to the current real top-level stack; `BOOT_COMPLETED` and `MY_PACKAGE_REPLACED` start the existing authoritative-address/grace-managed service without racing a cold DataStore value; the exported Gateway Provider synchronously invokes one idempotent app-owned Koin bootstrap before publishing; and BLE discovery is a 12-second sweep that stops when Connected or the Connections lifecycle pauses, with the perpetual indeterminate progress animation removed. Focused navigation, scanner, receiver, Provider/Koin, service, and app tests pass. The JDK-21/en-US full `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile :app:lintFdroidDebug :app:lintGoogleDebug :app:bundleGoogleRelease --continue` gate completed 2,180 actionable tasks (428 executed, 11 from cache, 1,741 up-to-date); formatting, both Debug assemblies, tests, `allTests`, Desktop/JVM, shared KMP, iOS Simulator compilation, both Debug lints, Google Release R8/Lint Vital/AAB packaging, and cloud-runtime guards passed, and the command was nonzero only for six pre-existing findings in unmodified BLE (3), domain (1), model (1), and network (1) sources. The final version `1.0.6 (7)` Google arm64 Debug APK is 51,393,739 bytes, SHA-256 `8BC272A926D58AE811B63951707B0BF233D5BBC68F23A5300058607B9652C265`, and passes 16 KiB zipalign. It was data-preserving installed on three Android 16 phones with matching installed hashes and retained first-install timestamps; the two phones with one existing radio each recovered MeshService/FGS and Stage 2 without a manual launch after package replacement, while the no-radio phone stopped after the startup grace. Cold Provider stress started the complete graph 100/100 times with zero Koin/fatal failures while all 100 untrusted shell queries remained rejected. All 12 initial manifest-deep-link cases plus three later Settings -> Channels -> Back checks retained one PID and rendered correctly. On the affected Samsung, the 70-second Connections average fell from the reproduced 71.1% one-core/126 rendered frames per second to 4.69%/8.44; manual scan ended by the bounded timeout and HOME stopped it without losing the connected session. At the user's request the planned 185-minute soak was ended early after 59 complete one-minute samples per phone (minutes 0-58) and 12/12 UI events: every phone stayed online with one PID, all safety error counters remained zero, D2/D3 retained FGS and Connected at checkpoints, and there was no monotonic thread or short-run memory failure. Complete-window CPU averaged 0.27%/7.95%/16.42% one-core on D1/D2/D3; D3's intermittent energy cost remains an unproven Profile-build candidate, not a source root cause. This is single-radio-per-phone Debug evidence, not a three-hour, Doze, Profile/Release-device, two/four-radio, RF-send, remote-receipt, signed-release, or store result; common changes compiled for Desktop/JVM and iOS Simulator but those hosts were not device-tested.

> - **Android Multi-Node Channel Hub UI/UX and Three-Phone Status (2026-08-25):** The two channel-hub proposals were implemented as a bounded read-only per-endpoint conversation projection aggregated by a root `FleetChannelsRepository`; the existing independent Room/DataStore/Koin/BLE/packet graphs remain the only owners of reads and writes. Android Messages now exposes an All view plus endpoint tabs and polished endpoint/channel cards with connection, role, lock, preview, time, unread, appearance, and empty-state affordances. Endpoint/generation-addressed routes use an exact fail-closed scope host, and runtime-token source ownership prevents a stale session from removing its replacement. Desktop retains its single-radio UI through an explicit no-op binding, while shared KMP/iOS Simulator compilation passes. Device profiling found that the common `AnimatedConnectionsNavIcon` chained one-second glows under normal packet activity; replacing it with the existing static icon reduced the same 20-second static Channel Hub render counts from 366 to 2 frames on the connected Samsung and 62 to 8 on the OPPO without changing BLE or packet behavior. Focused tests and changed-module formatting/Detekt pass. The JDK-21/en-US full gate completed 2,000 actionable tasks; after it exposed and the source corrected the Desktop no-op binding omission, the 177-task Desktop formatting/Detekt/test rerun passed all 32 tests. Android Debug builds/tests/allTests, both lints, Desktop/JVM, shared KMP, and iOS Simulator compilation are green; root Detekt remains nonzero only for six pre-existing findings. The final 52,672,620-byte Google arm64 Debug APK `1.0.6 (7)`, SHA-256 `F6E92B9C55D5837DCA920B8A5CD22713B770CFA76E693FBFA6B0DE6CE78028D8`, passes 16 KiB zipalign and has the same installed hash on all three Android 16 phones. At the user's request a planned three-hour run ended after minutes 0-107: 324/324 samples and 12/12 UI checkpoints passed, all three phones retained one PID, the two one-radio phones retained Connected/FGS 108/108, fatal/ANR/death/projection counters stayed zero, and first/last RSS windows plus bounded threads showed no monotonic leak. CPU averaged 0.23%/3.85%/4.51% one-core on the no-radio Samsung/connected Samsung/connected OPPO. This is single-radio-per-phone Debug evidence only; no two/four-radio-per-phone, RF-send/remote-receipt, Doze, Profile/Release-device, signed-release, store, Windows-device, or iOS-device claim is made. Full report: `ANDROID_MULTI_NODE_CHANNEL_HUB_UI_UX_IMPLEMENTATION_AND_THREE_PHONE_REPORT_2026-08-25.md`.

> - **Android Three-Language First-Launch Status (2026-08-21):** A genuinely fresh Android install now opens with the same English / Traditional Chinese / Japanese selector as the released NTsocial Android parent before the existing MeshLink introduction. The background and US, Taiwan, and Japan flag PNGs already present in this GPL repository are byte-identical to the authorized read-only parent references; the selector preserves the parent's 30%-from-right placement, 360-dp panel limit, 24/16/12-dp spacing, 48-dp rows, 24-dp flags, 2-dp corners, blue radio states, order, labels, and immediate select-and-advance behavior. The locale write is awaited before AppCompat recreates the Activity. One authoritative DataStore launch snapshot prevents fallback values from misclassifying an upgraded install; any install with the existing introduction-complete flag bypasses the new selector, and an interrupted first run with a persisted locale resumes the original introduction. Android locale configuration and the in-App Settings picker expose exactly `en`, `zh-TW`, and `ja`; Settings now always contains `App language` instead of delegating API 33+ users to a separate system screen. The selector and Settings UI are Android source-set behavior. Shared changes are limited to the atomic launch-preference contract, its fakes/tests, and three label resources; Desktop/JVM, shared KMP, and iOS Simulator compilation remain green with no Windows or iOS host-language flow added. Focused preference/intro tests and changed-module Detekt pass. The JDK-21/en-US full `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile` plus both Android Debug lints completed 1,960 actionable tasks (614 executed, 3 from cache, 1,343 up-to-date) in 22m55s; formatting, tests, both Android Debug assemblies, both lints, Desktop/JVM, shared KMP, and iOS Simulator compilation passed. The root command was nonzero only for six pre-existing findings in unmodified BLE (3), domain precise-location sharing (1), model (1), and network (1) sources; this run corrects the preceding five-finding inventory by recording the already-present domain `LongMethod`. No physical-device first-launch, visual, accessibility, or locale-switch smoke test was performed for this source-only follow-up.

> - **Android Kotlin List Compatibility Status (2026-08-21):** To remove the Google Play Android 15 Kotlin/JDK API-collision crash path, the two production commonMain tail-removal sites now use the semantically equivalent `removeAt(lastIndex)`, and the remaining iOS runtime test queue uses `removeAt(0)`; no Kotlin source call to `removeFirst()` or `removeLast()` remains. The Android/Windows/iOS shared logic is otherwise unchanged. Focused DataStore, domain, and iOS runtime JVM tests pass, including the affected channel trimming and drain paths. The JDK-21/en-US full `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile` gate, both Android Debug lints, cloud-runtime verification, Lint Vital/R8, and Google Release bundle packaging completed 2,137 actionable tasks (376 executed, 1,761 up-to-date) in 15m46s; the command was nonzero only for six pre-existing findings in unmodified BLE (3), domain precise-location sharing (1), model (1), and network (1) sources. The final version-code-7/version-1.0.6 Google Release AAB passes official bundletool 1.18.3 validation; all three DEX files contain no `java.util.List.removeFirst/removeLast` method reference and the Play-reported `ChannelSetDataSource...ExternalSyntheticApiModelOutline0` class is absent. The 24,462,232-byte AAB has SHA-256 `A188B65F5EEEF7C37E923610745E2B28A77EC72FF3089D7865BE1A73D35E10D0` and remains locally unsigned because no upload keystore is present; this is source/build evidence, not a signed Play upload or Play acceptance result.

> - **Android Stage-2 Readiness Regression Status (2026-08-09):** Commit `524778151` moved canonical `Connected` publication after awaited exact-session startup administration while the shared packet worker still required canonical `Connected`, creating a Stage-2 cycle that hid the Messages QR FAB and disabled every composer. `PacketHandlerImpl` now distinguishes ordinary `WAIT`, exact-control `READY`, and stale/Gateway `REJECT`: ordinary history work remains queued during `Connecting`, configured exact-session control can bypass it, canonical `Connected` wakes retained work, Gateway stays canonical-Connected-only, rejected dequeue ownership is cleaned non-cancellably, and response registration occurs only for the actual in-flight item so zero-ID QueueStatus cannot complete a waiting packet. Focused PacketHandler tests pass 20/20 and changed-module Spotless/Detekt/data tests pass. The required root `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile` plus both Debug lints completed 1,960 tasks; only the five documented pre-existing Detekt findings in unmodified BLE (3), model (1), and network (1) sources remain. A clean install of the 52,299,121-byte Google arm64 Debug APK (SHA-256 `04DF73D0911CF5FC2268654DF33110B4B03B8BF62E349508226E0405D5DD5846`) on one Android 16 SM-S9280 connected to a standard Meshtastic radio, completed all exact startup QueueStatus waits, remained free of stall/timeout/forced-reconnect/disconnect logs for more than five minutes, exposed and opened the QR scanner, generated the channel QR, and accepted then cleared local composer input without sending it. This is one-device Debug/source evidence, not a Play-delivered artifact, RF delivery proof, or Windows/iOS device validation; the common code compiled through the shared KMP gate.

> - **Android Channel Wait and Parent Route-Catalog Status (2026-08-17):** Shared KMP channel apply now distinguishes acknowledged, explicit decoded `Routing` rejection, and unconfirmed outcomes. Node reboot, no response, malformed response, queue/session loss, 30-second readback timeout, and exact-context rotation remain informational `VERIFICATION_PENDING`; protocol/readback terminal outcomes are an explicit NAK or a complete same-context mismatch, while local validation may reject an invalid set before sending. QR replace and both Channels paths show non-modal, non-red 30-second restart/reconnect guidance, permit navigation away, and finish an admitted apply non-cancellably; pending ingress remains fail closed. Android Gateway v2 status, channels, and route validation now consume one atomic channel-set/radio-generation snapshot. The separately authorized Android parent selects a coherent bindable v2 Provider catalog instead of a legacy-v1-first candidate, validates status -> channels -> status with bounded retry, preserves last-known-good data across transient probes, and refreshes the LoRa dialog with an explicit loading state. Main focused tests completed 263 tasks; the full `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile` plus both Debug lints completed 1,960 actionable tasks successfully, with only the five documented pre-existing Detekt findings. Parent focused tests pass 27/27 and Debug assembly passes. Exact F-Droid arm64 MeshLink 1.0.3 (4) and parent 1.5.5 (35) Debug APKs were installed with matching hashes on an SM-S9280 and CPH2695. On the Samsung, the existing node was discovered, connected, and completed Stage 2; two reversible manual secondary-channel updates each showed the neutral 30-second restart/reconnect guidance, allowed system Back during apply, completed reboot/reconnect/fresh readback in the background, restored the original channel name, and produced no terminal popup, rejection, mismatch, or fatal event. After both event storms the parent retained five online routes. One reversibly bound synthetic parent envelope produced 9 accepted Gateway receives and 9 enqueue/dequeue/`to_radio` dispatches with zero rejects or fatals, after which the binding was removed. This proves connected channel apply/readback and local Gateway dispatch to the radio queue, not RF airtime or remote receipt; second-node remote receive and Windows/iOS device runs remain outstanding.

> - **Android QR Secondary-Replacement and Immediate-Dismiss Status (2026-08-20):** An `add=true` channel QR now permits an explicit secondary-replacement mode after the current primary and LoRa state are loaded. The current primary and radio settings are preserved, explicitly released secondary slots are filled in place from selected incoming channels, internal disabled holes are treated as released, and acceptance is disabled whenever production normalization would move a retained secondary to a different numeric slot. Full-config QR replacement retains its existing complete channel/LoRa semantics, while ordinary Add behavior remains additive. Add and secondary-replacement requests carry null LoRa write intent; the reliability manager captures current LoRa only for normalization and exact readback and never emits `set_config` for a null request, even if the local LoRa flow changes during channel writes. Explicit non-null LoRa replacement still writes the exact requested config. Before Accept, the dialog now shows localized guidance that the node normally restarts, reconnect/verification takes about 30 seconds, the returned channel list may not update immediately, and completion continues in the background. Accept still synchronously claims the operation and then closes the dialog in one action; the admitted ACK/commit/fresh-readback transaction continues non-cancellably, terminal or invalid results use the global alert surface, and a second QR dialog cannot be consumed by the first operation's completion. The shared Compose change directly affects Android and the Desktop/Windows surface; the current iOS shell does not expose this dialog, but the KMP/iOS compilation gate remains green. Focused dialog/ViewModel/domain tests pass, including pre-Accept guidance and exact confirm-before-dismiss ordering. The JDK-21/en-US full gate plus both Android Debug lints completed 1,960 actionable tasks (592 executed, 6 from cache, 1,362 up-to-date) in 19m49s; formatting, tests, Android Debug assembly, both lints, Desktop/JVM, shared KMP, and iOS Simulator compilation passed, and the root command was nonzero only for the same five documented pre-existing Detekt findings in unmodified BLE (3), model (1), and network (1) sources. The user's physical-device test confirmed the preceding Replace and immediate-dismiss behavior before this pre-Accept guidance was added; the new guidance itself has not yet received a physical-device visual check, and no new camera, connected-radio, or RF validation was performed for this follow-up.

> - **Windows Track Status (2026-07-23):** The existing `:desktop` Meshtastic client is now a high-completion, independently usable Windows-branded `NTsocial MeshLink`. Window/taskbar/tray/toast identity, installer metadata, blue butterfly assets, Windows theme, fiber background, translucent shell, and the three-second process-cold-start splash are implemented. The shared first-release Connections UI remains Bluetooth-only even though TCP/Serial/USB/BLE backend capabilities remain in the desktop code. No `NTsocial_Windows` IPC, Windows Service, Authenticator, code signing, or proprietary parent-App logic has been implemented; do not claim that product-to-product integration exists until a separate scoped contract is built and tested.

> - **iOS Track Status (2026-08-07):** The current uncommitted iOS worktree from base HEAD `988d3327e45772e73dd2147ee7fffe4a26d370a6` implements a static `MeshLinkKit` framework; SwiftUI/Xcode host with source Privacy Manifest and AppIcon asset catalog; real Koin/Room/repository/orchestrator stack; Kable/CoreBluetooth restoration-configured transport; Security.framework randomness; file-backed DataStore; Room durable replay; App Group v1 SQLite mailbox; private restart-stable ledger; shared-Keychain HMAC; payload-free Darwin hints; deep-link handoff; and a focused connection/integration UI. Radio readiness, route/admission, and inbound identity bind the exact selected/active session, active per-radio database, completed config/readback/snapshot, Bluetooth/transport/App state, history epoch, and channel fingerprint. Replacement synchronously revokes callbacks, quiesces ingress/child/outbound/status/log work, hydrates the selected database/cache, and preserves expected epoch through admin/readback/packet dequeue to synchronous transport send, including same-address/same-transport reconnect. Manual/QR apply, public protected reconcile, and built-in provision use one serialized contract: validate/ensure → invalidate ingress → exact-session mutation → correlated fresh readback → activate verified identity. Readback reuses firmware `69420` under a host-exclusive owner/token after FULL Stage 2 and a dedicated completion flow; stale/parallel FULL or generic-generation events cannot satisfy it. Durable accepted Gateway rows retain source identity, and actual drain revalidates exact ingress plus slot/PSK/LoRa identity through matching firmware QueueStatus; a reused numeric slot fails closed. `READY` drains immediately, uses one 500/1,000/2,000-ms three-attempt retry job, and schedules a delayed continuation after the 64-command budget. `overlay_epoch_state` preserves per-epoch high-water beyond retention, and exact accepted-ledger replay reconstructs a result-publication crash without second local admission. The separately authorized `NTsocial_release` adapter has restart-stable pending/multipart correlation, deterministic current/historical source resolution, durable gap/poison recovery, and authenticated `enqueueNativeBroadcastText`; its composer remains deferred, focused tests pass 27/27, full SwiftPM passes 668/668, and release source build is green. Current-source focused slices pass 135/135 (domain 16, data 104, runtime 15), and `:core:gateway:jvmTest` passes 36; deterministic tests and the final bounded audit found no reproducible P0/P1 in the session/readback/mutation/dispatch boundaries. The only known open source P2 is fail-closed same-epoch liveness if an admitted exact readback times out/cancels and firmware never returns; reconnect/new epoch recovers, with no wrong-channel or disclosure path. The current root full gate is green, root Detekt is nonzero only for five specified pre-existing findings, and fresh signing-disabled Simulator Debug plus generic-iphoneos Release quiet builds exit 0. No signed archive, physical-device entitlement/BLE/restoration, connected-radio, LoRa/RF/remote-receipt, permanent-background, TestFlight, or App Store-readiness proof exists. Kotlin/Native iOS test link/run tasks remain disabled by convention, so native evidence is compilation only.

> - **iOS Final Host Evidence:** The 135/135 split is domain reliability 16; data 104 (config flow 32, connection 21, provision 11, packet handler 17, Gateway 22, real-lock integration 1); and runtime 15 (including durable source/liveness 2). Spotless, all five changed modules' Detekt, iOS Simulator Arm64 compilation/framework link, and diff hygiene pass. The en-US/JDK 21/one-worker root `assembleDebug test allTests kmpSmokeCompile --continue` gate completed in 2m with 1,406 actionable tasks (333 executed, 1 from cache, 1,072 up-to-date); native iOS test link/run remained skipped. Root Detekt reports only BLE `JvmDesktopBluetoothPairingService` line 154 `TooGenericExceptionCaught` and lines 143/188 `ThrowsCount`, model `NtsocialGatewayIdentity` line 168 `MagicNumber`, and network `BleRadioTransport` line 246 `ThrowsCount`. A final Koin cycle between `NtsocialGatewayRepositoryImpl` and `IosDurableMessageQueue` was removed with cycle-free `GatewayIngressSessionGate.activeSessionEpoch`. Fresh Simulator Debug Derived Data `/tmp/ntsocial-ios-final-fixed.2fROK9` built signing-disabled with quiet exit 0/zero output, installed and cold-launched on `Codex iPhone 17` (`E3249756-57AF-4D9C-AA2B-3332E9309529`) as PID 67524, and returned the same PID two seconds later. Fresh signing-disabled generic-iphoneos Release Derived Data `/tmp/ntsocial-ios-final-device.YaTk5N` also had quiet exit 0/zero output; bundle `1.0.0`/build `1` contains `Assets.car` and `PrivacyInfo.xcprivacy`. These are source/simulator/unsigned device-architecture results only.

> - **iOS Shared Product-Shell/UI Parity Phase-1 Status (2026-08-29):** The former two-tab Connection/Integration engineering shell now uses the same shared Compose `MeshtasticAppShell`, adaptive Navigation 3 multi-back-stack, butterfly treatment, and conversations, nodes, MeshCore, settings, and Bluetooth-connections destinations as Android/Desktop. iOS Settings is no longer empty: it renders the real shared radio/channel/module hierarchy plus theme, homoglyph, database-cache, About/version, Wi-Fi routes, remote administration, and an embedded host-owned Apple Gateway readiness section; unsupported OTA, backup/import/export, notifications, file, CoreLocation, compass, and the then-unimplemented fleet projection remained hidden or explicitly fail closed. Foundation date/relative-time and regional formats, clipboard, URL opening, enum preference contents, feature deep links, iOS Connections Koin bindings, Japanese Gateway strings, and zh-TW/ja Bluetooth empty-state strings are implemented. At that snapshot the Apple exact-session/Room/DataStore/Kable/App Group/Keychain/Gateway runtime remained single-radio; the 2026-08-30 multi-node phase below supersedes only that limitation. MeshCore remains an explicit transport-pending UI only. Final focused iOS/Windows validation passed 368 tasks, iOS runtime tests pass 17/17, both Simulator Debug and arm64 Release frameworks link, and Desktop tests pass. The final JDK-21/en-US root gate completed 2,015 tasks; formatting, both Android Debug assemblies, tests/`allTests`, Desktop/JVM, KMP/iOS Simulator compilation, cloud guards, and both Android lints passed, with nonzero exit only for the six recorded pre-existing Detekt findings. A final post-localization 749-task cross-platform replay and 161-task arm64 Release link pass. A fresh signing-disabled Simulator Debug build, its final-source incremental localization rebuild, and a fresh generic-iphoneos Release build all have quiet exit 0/zero output; the localized Simulator App cold-launched as PID 22251 and retained that PID after three seconds. The Node dependency chain still reports Coil/Skiko alignment and Vico `Paint.shader` linker diagnostics despite successful links, so node-chart rendering remains a physical-device/dependency-alignment gate. No MeshCore transport, signed archive, physical-device BLE/restoration/entitlement, connected-radio, LoRa/RF/remote-receipt, background-permanence, TestFlight, or App Store claim is made. Full report: `IOS_UI_PARITY_PHASE1_IMPLEMENTATION_REPORT_2026-08-29.md`.

> - **iOS Meshtastic Multi-Node Phase-1 Source Status (2026-08-30):** iOS now consumes the shared durable fleet catalog and serialized bootstrap for at most four distinct Meshtastic BLE endpoints. The first endpoint preserves the existing root compatibility facade and is the only Apple Gateway owner. Every secondary endpoint owns an independently named Room database, radio/config DataStore set, Koin scope, repository/service/packet graph, exact transport generation, and durable queued-message drain; shared address-keyed BLE ownership and exact runtime-token source registration prevent stale sessions from removing or publishing into replacements. Connections now registers, connects, disconnects, selects, and removes fleet endpoints with Android-parity cards, capacity, primary/status labels, and address suffixes. Conversations provides All plus endpoint views, while Nodes, Settings, Channels, and endpoint conversations use exact endpoint scopes and fail closed when an endpoint/generation is stale; switching endpoints resets the current feature to its root. App-global preferences stay shared. The focused source gate passes iOS runtime 19/19, radio-fleet 8/8, and prefs 36/36 JVM tests; changed iOS/Connections Spotless and Detekt pass, Simulator Debug and arm64 Release frameworks link, a fresh signing-disabled Simulator Debug app installs/cold-launches with a stable PID, and a fresh generic-iphoneos Release host build exits quietly with code 0. The JDK-21/en-US root gate completed 2,015 tasks (381 executed, 1,634 up-to-date): formatting, both Android Debug assemblies, tests/`allTests`, Desktop/JVM, shared KMP/iOS Simulator compilation, cloud guards, and both Android lints passed; exit was nonzero only for the six recorded pre-existing Detekt findings. Kotlin/Native iOS tests remain compile/link-only by convention. MeshCore multi-node, cross-endpoint send scheduling/bridging, secondary Apple Gateway exposure, physical concurrent two/four-radio BLE, independent connected-radio mutation, restoration/background, LoRa/RF/remote receipt, signing, TestFlight, and App Store readiness are not proven or claimed. Full report: `IOS_MULTI_NODE_PHASE1_IMPLEMENTATION_REPORT_2026-08-30.md`.

> - **iOS Simultaneous Multi-Node Production-Graph Remediation Status (2026-08-31):** Git comparison against Android remediation `5b856b5fa468327f7fc9a24eebe5d36dfc86b2da` confirmed the iOS secondary-endpoint Koin module retained the same defective constructor-reference registrations for constructors requiring qualified `ProcessLifecycle`/`ServiceScope` values or Kotlin `Lazy<T>` dependencies. The iOS graph now uses explicit scoped factories across connection, packet, router, configuration, and channel-reliability ownership; secondary Apple Gateway access is explicitly bound to a fail-closed repository; and graph wiring is marked complete only after successful activation-root resolution and radio-buffer reset. A Kotlin/Native production-graph regression resolves a real secondary `MeshConnectionManagerImpl` and verifies fail-closed Gateway binding. Focused iOS validation completed 398 tasks successfully: runtime formatting/Detekt/JVM tests, Simulator and arm64 compilation, Simulator test-source compilation, and Debug framework link. The required full gate completed 2,018 tasks (186 executed, 1,832 up-to-date); formatting, both Android Debug assemblies, tests/`allTests`, Desktop/JVM, KMP/iOS compilation, and both Android lints passed, with exit 1 only for the six recorded pre-existing Detekt findings in unmodified BLE (3), domain (1), model (1), and network (1) sources. A fresh signing-disabled Simulator Debug Xcode build installed and cold-launched on `Codex iPhone 17` as PID 12071 and retained that PID. Kotlin/Native tests remain run-disabled by convention, so the new native regression is compile-only; no physical concurrent iPhone BLE/Stage-2, independent control, restoration/background, RF/remote-receipt, signed-release, TestFlight, or App Store claim is made. Full report: `IOS_SIMULTANEOUS_MULTI_NODE_PRODUCTION_GRAPH_REMEDIATION_REPORT_2026-08-31.md`.

> - **iOS Three-Language First-Launch Status (2026-08-30):** A fresh iOS install now uses the exact shared Android first-launch language selector for English, Traditional Chinese, and Japanese, including the same NTsocial background and flag assets, order, panel placement, spacing, dimensions, blue radio states, and immediate select-and-advance behavior. The iOS root waits for one atomic launch-preference snapshot, persists only `en`, `zh-TW`, or `ja` with an awaited write, bypasses the selector when a locale already exists, and supplies that locale to the live shared Compose resource tree. iOS Settings now exposes the Android-like `App language` row and three-option radio dialog. The Xcode host declares `en`, `ja`, and `zh-Hant` and ships localized Bluetooth permission strings. Android continues to consume the now-common selector without behavior changes; Desktop does not adopt the first-launch gate. Intro tests pass 9/9, focused Settings tests pass 14/14, iOS runtime tests pass 19/19, changed-module formatting/Detekt and Android/iOS compilation pass, and a fresh signing-disabled iOS 26.5 Simulator build installs and cold-launches with a stable PID while visibly rendering the selector and packaging all three `.lproj` resources. The unfiltered Settings JVM UI suite still has unrelated pre-existing `DebugSearchTest` semantics failures. No signed archive, physical-device visual/accessibility/Bluetooth-permission check, TestFlight, or App Store-readiness claim is made. Full report: `IOS_THREE_LANGUAGE_FIRST_LAUNCH_IMPLEMENTATION_REPORT_2026-08-30.md`.

> - **iOS USB Physical-Device and Parent-Integration Status (2026-08-30):** A diagnostic entitlement-stripped `NTsocial MeshLink` 1.0.0 (1) build was signed, installed, launched, gracefully terminated, and relaunched on a wired iPhone 15 running iOS 26.6.1 alongside the existing `NTsocial` 1.0.0 (1) parent. Parent/companion foreground changes retained both processes, `ntsocial-meshlink://process` routed to the existing companion process, the private Room database remained schema 43 with a clean integrity check and retained `history_epoch_v2`, and no MeshLink crash report was present. The localized Bluetooth permission card rendered, but the user was away, Bluetooth was reported off, and two XCUITest attempts timed out while iOS enabled automation before any test method ran; BLE, connected-radio, Stage 2, RF, and remote receipt remain untested. The focused MeshLink/parent baseline passed 126/126 tests. Real Apple Gateway communication is blocked by signing: the local profiles for both installed Apps omit the required App Group, the parent Debug configuration uses its empty personal entitlement file, a normal MeshLink entitled build fails profile validation, and CoreDevice cannot resolve `group.com.ntsocial.meshlink.gateway`. The runtime correctly fails closed, so no product code was changed and the diagnostic build is not interoperability or release evidence. Both exact bundle IDs need eligible-team profiles carrying the same App Group and shared Keychain group before the two-App mailbox/HMAC/Darwin round trip can be tested. Full report: `IOS_PARENT_INTEGRATION_USB_PHYSICAL_DEVICE_TEST_REPORT_2026-08-30.md`.

> - **iOS AppIcon Visual-Scale Remediation Status (2026-09-01):** The iOS `AppIcon-1024.png` now composites the established shared Meshtastic-green NTsocial butterfly on black with a 960-by-960 foreground canvas instead of enlarging the Android store icon with its adaptive-icon safe margin already baked in. At the same crop-detection threshold, the visible mark increased from 548-by-426 to 798-by-614 pixels while retaining a 1024-by-1024, 8-bit RGB, no-alpha, embedded-sRGB App Store source; Android and Desktop assets are unchanged. A signing-disabled iOS Simulator Debug Xcode build compiled the asset catalog without AppIcon warnings, installed on `Codex iPhone 17`, launched, and returned to SpringBoard with the larger mark visibly centered, uncropped, and balanced inside the system mask. The JDK-21/en-US `spotlessApply spotlessCheck detekt assembleDebug test allTests kmpSmokeCompile` plus focused iOS JVM, Simulator/arm64 compilation, test-source compilation, and Debug framework-link gate completed 1,782 actionable tasks (100 executed, 1,682 up-to-date); formatting, Android Debug assembly, tests/`allTests`, KMP/iOS compilation, and framework link passed, with exit 1 only for the six recorded pre-existing Detekt findings in unmodified BLE (3), domain (1), model (1), and network (1) sources. This is source/Simulator visual evidence only; no physical-device, signed archive, TestFlight, or App Store review was performed.

> - **Four-Phone Clean Debug Redeployment Status (2026-09-01):** The exact MeshLink packages were removed from two connected Android 16 phones and two connected iPhone 15 devices, absence was verified on each host, and current-source Debug artifacts were then installed from scratch. Samsung SM-S9280 and OPPO CPH2695 now run Google arm64 Debug `1.0.8 (9)`; both installed `base.apk` files exactly match the 51,640,587-byte local APK SHA-256 `719589AB776C611A6FF52DF3B333725EFDE8A33B90612E97D242D522B00E5113`, cold launch returned `Status: ok`, and both processes retained their initial PIDs through the follow-up check. Both iPhone 15 devices now run the newly built arm64 Debug `1.0.0 (1)` containing the enlarged AppIcon; each install, launch, and follow-up process lookup succeeded with a stable PID. The iOS build uses the existing diagnostic personal-team profile with `get-task-allow=true` and no App Group entitlement, so Apple Gateway remains fail closed. This proves four-device clean-install and immediate startup integrity only; physical icon appearance, BLE permission/scan, concurrent radios, Stage 2, channel mutation, restoration/background, LoRa/RF/remote receipt, release signing, and store delivery remain untested by this deployment.

> - **iOS Apple Gateway Stable-Channel Binding and Entitlement Source Status (2026-09-02):** This coordinated MeshLink-iOS and `NTsocial_release`-iOS source correction supersedes the source conclusions, but not the retained device evidence, in the 2026-08-30 report above. MeshLink Debug and Release use the same Apple Gateway entitlement file; parent Debug now uses a Gateway-only entitlement file and parent Release retains its full entitlement file. All four source configurations declare App Group `group.com.ntsocial.meshlink.gateway` and shared Keychain access-group suffix `com.ntsocial.meshlink.gateway`. These declarations cannot grant capabilities: both exact bundle IDs still require eligible-team provisioning profiles containing the same groups before a signed two-App run can work. The parent Channel Info UI no longer accepts an arbitrary numeric slot for MeshLink; it offers only current catalog rows that advertise `SEND_NTSOCIAL_ENVELOPE`, persists the opaque stable source identity, and keeps the slot only as last-known display/compatibility metadata. Every Apple Gateway send resolves that source against the fresh catalog, route token, capability, and radio generation; source removal or same-slot reuse fails closed before a command is published. Social, reconcile, probe, overlay/delivery receipt, and ATaK reply payloads carry the source identity through the provider boundary. While the MeshLink host is foreground-active, the legacy-primary Apple Gateway owner renews only READY projections every half of the 120-second route TTL; leaving the foreground stops renewal and does not make a stale route authoritative. Secondary iOS radio endpoints remain fail closed for Apple Gateway. Focused MeshLink Gateway/runtime tests pass 14/14. The JDK-21/en-US full gate completed 2,018 actionable tasks (100 executed, 1,918 up-to-date): formatting, both Android Debug assemblies, tests/`allTests`, shared KMP/iOS Simulator compilation, and both Android Debug lints passed; exit 1 was only the six recorded pre-existing Detekt findings in unmodified BLE (3), domain (1), model (1), and network (1) sources. The gate also exposed two stale Compose JVM test classes; their test-only assertions now use resource strings and the correct merged/unmerged semantics boundary, after which messaging passed 25/25 and settings 62/62. Signing-disabled clean Xcode builds pass for Simulator Debug and generic iPhoneOS Release; the latter linked the arm64 static framework after 243 Gradle tasks (48 executed, 195 up-to-date). The parent source-frozen parity gate passed all 13 steps in 681 seconds, including 785/785 SwiftPM tests, host interop, Simulator/build-for-testing/Release-XCUITest, and source-state checks. This entry does not claim a signed physical mailbox round trip, connected-radio admission, RF airtime, remote receipt, multi-endpoint Gateway, TestFlight, or App Store evidence.

> - **iOS Restored BLE Session Recovery Remediation Status (2026-09-03):** The physical iPhone reconnect regression was traced to an OS-restored MeshLink GATT link that kept the single-owner Meshtastic PhoneAPI connected and non-advertising while Kable 0.42 reconstructed its wrapper as logically disconnected; the fallback Apple scan also supplied Kable's unsupported address predicate and swallowed its rejection. Saved CoreBluetooth identifiers now use direct reconstruction plus protected FROMNUM subscription verification, transfer the same verified peripheral/scope into the transport, and use a bounded stale-restoration release/settle/fresh-wrapper retry when CoreBluetooth does not replay `didConnect`. Prepared sessions carry exact device-instance ownership; transport and pairing-UI cancellation discard unclaimed sessions non-cancellably, a 30-second lease closes abandoned sessions, stale cleanup cannot remove a newer replacement, and a same-address race re-reads the repository's exact staged owner before transport handoff. Transport close also boundedly joins an in-flight handoff before disconnect; a slow handoff receives a deferred second disconnect that repeated close calls cannot cancel. Apple scan fallback keeps the service filter and drops the unsupported address predicate while exact identifier matching remains authoritative; Android/Desktop retain their prior OR-filter behavior. Connections rethrows coroutine cancellation so lifecycle/navigation cancellation no longer surfaces as `Job was cancelled`. The signed, App-Group/Keychain-entitled Debug build was data-preserving installed on both connected iPhone 15 devices. On the affected phone it released the stale link, reconnected to the exact saved node, sustained GATT writes/reads, recovered again after cold process restart, and later consumed indications while suspended without a crash/fatal. The entitled parent then retained two current, same-generation READY projections (`MediumFast` primary and `NTsocial` secondary), both send-capable. BLE JVM tests pass 47/47, Android BLE host tests pass 40/40, core-network known-device, cancelled-handoff, close-ordering, and deferred-cleanup regressions pass, iOS Simulator/arm64 compilation and the signed physical Xcode Debug build pass. The JDK-21/en-US one-worker full gate completed 1,946 tasks; formatting, both Android Debug assemblies, tests/`allTests`, Desktop/JVM, KMP/iOS compilation, and both Android lints passed, with exit 1 only for the six recorded pre-existing Detekt findings. A transient full-load Compose dialog-semantics failure did not reproduce in the isolated 49/49 suite or under either English or zh-TW. No RF/remote receipt, concurrent two/four-radio iPhone, manual ordinary-channel binding, Release/TestFlight/App Store, or post-fix Channels-screen visual claim is made. Full report: `IOS_RESTORED_BLE_SESSION_RECOVERY_REMEDIATION_REPORT_2026-09-03.md`.

> - **iOS In-Channel Meshtastic QR Scanner Status (2026-09-03):** The endpoint-scoped Channels destination now exposes an in-page QR scan action backed by a QR-only VisionKit `DataScannerViewController`. Its full-screen overlay reproduces Android's 70%-of-short-edge square, 60% black mask, 3-point white 40-point corners, and top-left close control; camera permission, denied/unavailable recovery, background cancellation, and en/zh-Hant/ja purpose and alert strings are present. A request-token bridge rejects duplicate or stale native callbacks, teardown releases the scanner, and scanned contents are never logged. A dedicated Channels-only iOS capability keeps the existing Nodes, Contacts, and Wi-Fi scanner surfaces inactive, avoiding legacy-primary routing from a selected secondary endpoint. Valid results enter the selected endpoint's existing shared Android `ScannedQrCodeDialog`, preserving its Add/secondary-replace/full-replace form order and exact-session reliability manager. Common Channel parsing now returns redacted failures; Android also exposes the new action through its existing scanner provider, while Desktop remains unsupported and hidden. Focused formatting, parser/runtime JVM tests, iOS Simulator framework compilation, Xcode Simulator Debug/Analyze, and the signed arm64 Debug host build pass. That final build was data-preserving installed on both connected iPhone 15 devices. On the iPhone with the connected Meshtastic node, physical XCUITest opened the scoped Channels action, accepted first camera permission, verified the close control was hittable, captured and visually confirmed the Android-parity reticle, closed it, and returned to Channels; the post-scope build repeated the same flow successfully in 18.345 seconds. The run proves the physical camera surface and navigation, not optical decoding of a presented QR, channel mutation/readback, RF delivery, Release/TestFlight, App Store review, or pre-A12 iPad support.

> - **iOS Native Outbound Queue Remediation Status (2026-09-03):** The reported one-way `1407` Android-to-`0809` iOS interoperability failure was traced to the iOS durable outbox, not Android receive filtering or the shared RF configuration. Normal primary-channel text stores a stable source-channel identity, but `IosDurableMessageQueue` treated every such row as Apple Gateway-owned; an older Gateway row waiting on its extra session gate then stopped the global drain and also stranded later direct messages. Durable queue entries now carry explicit Gateway provenance derived from the outbound private-app port or Apple Gateway origin client-message ID. Native rows drain ahead of gated Gateway work, while Gateway exact-session/source checks remain unchanged and malformed Gateway rows still fail closed. The focused queue suite passes 3/3, changed-scope formatting/Detekt passes, and the final signed arm64 Debug Xcode build was data-preserving installed on the physical iPhone connected to the `0809` radio. Its formerly all-queued native backlog reached the firmware (one `DELIVERED`; the burst remainder explicitly rejected as `RATE_LIMIT_EXCEEDED`), and fresh database evidence recorded iOS `!f2470f9e` primary messages `DELIVERED` plus Android `!835d30ae` primary text `RECEIVED`; the user manually confirmed the requested two-way device flow passed. Android code was not changed. Per the requested narrow validation, no root-wide gate, soak, range, Release/TestFlight, or store run was added. Full report: `IOS_OUTBOUND_NATIVE_MESSAGE_QUEUE_REMEDIATION_REPORT_2026-09-03.md`.

> - **Evidence Rule:** The user's verbal statement that Android is Play-listed has not been independently verified in this repository and does not supersede the retained Play build/submission evidence below. Never convert a store, signing, entitlement, device, RF, or delivery assertion into project status without reproducible evidence.

> - **Current Gateway v2 Build Status (2026-07-29):** From base HEAD `90eeaf92`, the additive native-text/Room-43 source passed 361 focused model/data/database/service tests covering stable identity, concurrent idempotency, normal Room-row creation, Provider/command parsing, receive-only rejection, and schema behavior. With the required en-US JVM locale, root `test allTests` completed successfully; `spotlessApply spotlessCheck`, `assembleDebug`, `kmpSmokeCompile`, and both Debug flavor lint tasks also passed. The 50,783,283-byte Google arm64 Debug APK has SHA-256 `94F4477B3D3BB0AD63B0EF229FA78549885363DBC69FE315D67C4677BAD5857B`. At that snapshot, root `detekt` was red only for the same three pre-existing findings in the unmodified `core/ble/src/jvmMain/.../JvmDesktopBluetoothPairingService.kt` (`TooGenericExceptionCaught` and two `ThrowsCount`); use the newer QR-interoperability status below for the current gate. An optional `assembleRelease` attempt remains red in the unmodified Widget module because release resources lack `colorControlNormal` and `widget_local_stats_preview`; this does not invalidate the green Debug artifacts or lints. The current build still lacks a retained Room 41-to-42-to-43 chain, direct on-device Provider/caller command acceptance, connected-radio admission/restart evidence, radio-history import, and remote RF reception.

> - **Android Channel-QR Interoperability Status (2026-07-31):** From base HEAD `af526f7e4`, the attached official Meshtastic eight-channel `add=true` QR was confirmed compatible with the existing URL/Base64/protobuf parser. Android live scanning now requests a 1280-by-960 4:3 analysis stream instead of relying on CameraX's VGA default, limits ZXing to QR with `TRY_HARDER`, delivers only the first scan/dismiss result on the main thread, and unbinds only its own use cases. ADD-mode import now reads the radio's positive `maxChannels` value with an eight-channel fallback, omits semantic duplicates/placeholders, and keeps excess unique channels visible but unchecked so an existing primary no longer disables acceptance of a full official QR. Channel writes are awaited sequentially before the local cache is replaced. Synthetic dense-QR tests pass in both Android flavors; eight-channel URL round-trip and shared capacity tests pass in KMP. Root `spotlessApply spotlessCheck assembleDebug test allTests`, `kmpSmokeCompile`, and both Debug flavor lint tasks pass with the en-US JVM locale. Changed-module Detekt passes; current root Detekt reports seven pre-existing findings only in unmodified BLE (3), model (1), network (1), and data (2) sources. The exact 51,948,716-byte Google arm64 Debug APK (`versionCode=2`, `versionName=1.0.1`) has SHA-256 `76B8F876CC4C2327B3C3E2274C0ECC09D06EA217C9154F204D385BA9D35368E6`; it was installed with `adb install -r -t` on SM-S9080, two SM-S9280 phones, and OPPO CPH2695. All four retained their prior first-install timestamps and read back the same installed base-APK hash. This is four-phone install integrity evidence, not camera-to-screen scan or connected-radio import evidence; those two checks remain required. The scanner is Android-only; the shared capacity/import correction also benefits Windows without changing Windows IPC or branding.

> - **Android Channel/GPS Reliability Status (2026-08-06):** From base HEAD `4151d6227a00dbfac6bec4e5afbab784c98fd4b1`, QR and local manual channel changes use one serialized verified path: ready admin session, full-slot edit, per-packet queue admission plus matching request/sender `Routing.NONE`, commit, and a fresh complete-config readback. UI success and snapshot mutation occur only after an exact readback. Per-radio channel protection is explicit opt-in and off by default; unattended repair is limited to provably missing secondary placeholders, attempts at most once per identity/generation, and fails closed for primary, LoRa, capacity, identity, generation, or content conflicts. Protected reconciliation runs before the built-in NTsocial provisioner; that legacy provisioner remains queue/cache based and is not claimed to have equivalent verified-transaction semantics. Device -> Position exposes the existing per-node phone-location preference and reconciles it against the connected node, fixed-position mode, Fine or Coarse permission, system location, App lifecycle, reconnect, and restart. Android uses the location foreground-service type only while explicit phone-location opt-in, permission, and system location are all active; opt-out stops the listener and no background-location permission is requested. Root `spotlessApply spotlessCheck assembleDebug test allTests`, `kmpSmokeCompile`, and both Debug flavor lints pass with JDK 21 and the en-US JVM locale. New or modified task sources add no Detekt finding; root Detekt still reports exactly the seven pre-existing findings in unmodified BLE (3), model (1), network (1), and data (2) files. Android lifecycle/FGS work is host-specific; shared channel contracts pass Desktop/JVM and KMP compilation without adding Windows IPC, UI, branding, or packaging behavior. No connected-radio, RF, or real-device GPS evidence was produced in this environment, so full QR/readback, reconnect repair, Android 11-17 lifecycle/permission, and second-radio position reception remain release gates.

> - **Windows Packaging Status (2026-07-23):** `:desktop:packageReleaseDistributionForCurrentOS` passes with a complete JDK 21 containing `jpackage.exe` (`BUILD SUCCESSFUL` in 7m42s). It produces unsigned `NTsocial MeshLink-1.0.0.exe` and `.msi` packages. MSI metadata is `ProductName=NTsocial MeshLink`, `Manufacturer=LiberaNt LLC`, menu group `NTsocial`, and upgrade UUID `6784A2DD-CE59-518B-AA15-C26302D6FA85`; application ID remains `com.ntsocial.meshlink.desktop`. Coexistence/upgrade was verified structurally from installer metadata, not by installing onto the machine. Scaling at 100/150/200%, light theme, tray re-show, and the final translucent-shell visual remain manual release QA.

> - **Android Packaging Status (2026-07-18):** The rendered-map module and its Google Maps, osmdroid, GeoPackage, and related native dependency path are removed. Both current arm64 debug APKs pass `zipalign -c -P 16`; the Google arm64 APK's five packaged ELF libraries all have 0x4000-aligned `PT_LOAD` segments. Both flavors also pass merged-manifest cloud-runtime guards. Repeat the audit after every native-dependency change and on the final signed delivery artifact.

> - **Parent Interop Status (2026-08-09):** Both MeshLink build types authorize only the exact `com.ntsocial.android` and `com.ntsocial.android.debug` packages. The release parent uses its approved production signer; the debug parent may use either the stable team-debug signer or retained development-debug signer `B578...3FF8`. The same three public digests are present in the Gateway `knownSigner` permission resources, and `NTsocial_release` symmetrically permits the exact MeshLink release/debug packages with their production, team-debug, or retained development-debug signer. This fixed matrix supports Debug/Release cross-pairing without a package wildcard; a debuggable MeshLink additionally retains exact nonempty current-signer equality for other local Debug-to-Debug builds. Empty identities, unknown signers, partial multi-signer matches, and arbitrary package names fail closed. The earlier local same-signer path was tested on three Android 16 arm64 phones with exact data-preserving APK updates and matching installed hashes: v1/v2 Provider queries returned one row, capability issuance succeeded, Android sender verification logged `trusted=true`, authorization logged `accepted`, and a deliberately nonexistent route produced the correlated metadata-only `COMMAND_REJECTED/invalid_route` event. NTsocial UI discovery, MeshLink launch, gateway toggle recovery, and parent-process restart recovery passed; an ADB-shell Provider query remained untrusted. No current Play-delivered signed pair, third-party F-Droid release signer, connected radio, RF delivery, or remote receipt was tested.

> - **Play Build Status (2026-07-18):** `:app:bundleGoogleRelease` passes R8, Lint Vital, dependency/manifest cloud-runtime guards, and AAB packaging without Google Cloud, Maps, Firebase, Crashlytics, Datadog, or ML Kit configuration. The locally generated AAB is unsigned because no upload keystore is present, so it is a release-pipeline artifact only and is not Play-uploadable. Never present an unsigned artifact or debug signature as Play-ready.

> - **Play Submission Status (2026-08-06):** The App is **not Production-submission-ready**. Known code/policy gates are: complete Android 11-17/API 30-37 real-device validation of the new explicit-opt-in location FGS path and resolve API 37 minimum-scope/location-button behavior; implement first-send terms acceptance plus in-app UGC reporting and effective block/ignore; remove or rewrite stale localized `analytics_notice` text; publish final policy URLs and update the in-app `privacy_url`; create and back up an upload key; synchronize Play app-signing certificates with both Apps' signer trust; prepare current store assets/Console declarations; complete cloud-free Internal-track device testing; and satisfy account-specific closed-testing/Production-access requirements. `docs/google-play/README.md` and `docs/google-play/06-first-play-launch-plan-zh-TW.md` are the submission source of truth.

> - **Device Validation (2026-07-10):** A normal NTsocial LoRa-enabled channel was tested on device through parent -> Provider/capability -> explicit command -> MeshLink -> connected radio firmware queue. The run used only port 256 and did not use AIDL or outbound 497. This proves local radio-queue acceptance, not remote over-air reception; the latter needs a second receiver or a return message.

> - **Three-Phone No-Radio Validation (2026-07-17):** Fixed debug APKs were clean-installed on `SM-S9080`, `SM-S9280`, and `OPPO CPH2695`, all Android 16/API 36 arm64 devices. Connections, Messages, Nodes, the then-present Map screen, MeshCore, Settings, onboarding, parent Provider status, explicit App launch, repeated foreground/background switching, and relevant log windows passed. English-keyboard text entry was re-tested successfully, and parent-local/BLE message synchronization was observed across phones. Rendered maps have since been removed, so the current cloud-free artifact still needs its own device smoke test. Because no Meshtastic or MeshCore hardware node was available, no current-run radio connection, LoRa transmission, RF reception, remote receipt, or MeshCore transport claim may be inferred from this result.
